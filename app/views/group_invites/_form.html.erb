<%= form_with(model: group_invite, local: true) do |form| %>
  <% if group_invite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_invite.errors.count, "error") %> prohibited this group_invite from being saved:</h2>

      <ul>
        <% group_invite.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sender_id %>
    <%= form.number_field :sender_id %>
  </div>

  <div class="field">
    <%= form.label :receiver_id %>
    <%= form.number_field :receiver_id %>
  </div>

  <div class="field">
    <%= form.label :accecpted %>
    <%= form.check_box :accecpted %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.number_field :group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
