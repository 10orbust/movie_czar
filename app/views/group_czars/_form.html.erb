<%= form_with(model: group_czar, local: true) do |form| %>
  <% if group_czar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_czar.errors.count, "error") %> prohibited this group_czar from being saved:</h2>

      <ul>
        <% group_czar.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.number_field :group_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :czar_count %>
    <%= form.number_field :czar_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
